# ============================================================================
# TaskFlow Pro — Environment Variables
# ============================================================================
# Copy this file to .env.local (for local dev) or .env (for Docker).
# See DEPLOYMENT.md for detailed descriptions of each variable.
# ============================================================================

# ── Supabase (Required) ─────────────────────────────────────────────────────
# Get these from: Supabase Dashboard → Settings → API

# Your Supabase project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase anonymous / public API key (safe for client-side)
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Supabase service role key (server-side only — NEVER expose to client)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ── Stripe (Required) ───────────────────────────────────────────────────────
# Get these from: Stripe Dashboard → Developers → API keys

# Stripe publishable key (safe for client-side)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Stripe secret key (server-side only)
STRIPE_SECRET_KEY=sk_test_...

# Stripe webhook signing secret (from Developers → Webhooks → endpoint → Signing secret)
STRIPE_WEBHOOK_SECRET=whsec_...

# Stripe Price IDs for subscription plans
# Create products in Stripe Dashboard → Products, then copy the Price ID
STRIPE_PRICE_PRO=price_1ABC...
STRIPE_PRICE_BUSINESS=price_1DEF...

# ── Email (Optional) ────────────────────────────────────────────────────────
# Resend is used for transactional email (invitations, notifications).
# Sign up at https://resend.com and create an API key.

RESEND_API_KEY=re_123abc...

# ── App Branding (Optional) ─────────────────────────────────────────────────
# Override these to white-label the app. All have sensible defaults.

# Application display name (shown in sidebar, login, emails, metadata)
NEXT_PUBLIC_APP_NAME="TaskFlow Pro"

# One-liner tagline (shown on marketing / onboarding pages)
NEXT_PUBLIC_APP_TAGLINE="Project management that gets out of your way"

# Path to logo (relative to /public or absolute URL)
NEXT_PUBLIC_APP_LOGO="/logo.svg"

# Canonical app URL (no trailing slash)
NEXT_PUBLIC_APP_URL=https://your-app.vercel.app

# Primary brand colour (hex)
NEXT_PUBLIC_PRIMARY_COLOR="#10b981"

# Secondary accent colour (hex)
NEXT_PUBLIC_ACCENT_COLOR="#059669"

# Support email shown to users
NEXT_PUBLIC_SUPPORT_EMAIL=support@taskflow.pro

# "From" address for transactional email
NEXT_PUBLIC_EMAIL_FROM="TaskFlow Pro <no-reply@taskflow.pro>"

# Domain used for sending email (must be verified in Resend)
NEXT_PUBLIC_EMAIL_DOMAIN=taskflow.pro

# ── Notifications (Optional) ────────────────────────────────────────────────
# Telegram notifications for admin alerts (new leads, signups, etc.)

# Telegram bot token (create via @BotFather)
TELEGRAM_BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11

# Telegram chat ID to receive notifications
TELEGRAM_CHAT_ID=-1001234567890
